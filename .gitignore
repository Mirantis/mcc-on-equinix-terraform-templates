# Local .terraform directories
.terraform

# .tfstate files
*.tfstate
*.tfstate.*
terraform.tfvars*

# temp credentials and configurations for equinix vlan infra
*ansible-inventory*
output.json
*ssh_key*
